# WellSync 設計概要 (v4) - Context Caching モデル

このドキュメントは、Health Connect API と Gemini API の Context Caching を組み合わせ、大規模なヘルスケアデータを効率的に分析するアプリ「WellSync」の設計書です。

---

## ■ アプリ名
**WellSync (ウェルシンク)**

---

## ■ ロジック構成

### 1. データ取得とキャッシュ戦略 (Context Caching)
* **ベースデータのキャッシュ:** 過去1ヶ月〜数年分の全データを Google のサーバー側にキャッシュ（Cache）として保持します。これにより、リクエストのたびに大量のデータを送信する必要がなくなります。
* **有効期限管理:** キャッシュには有効期限（TTL）を設定します（例：24時間）。ユーザーが頻繁にアプリを開く場合はキャッシュを維持し、コストを最適化します。

### 2. 差分更新ロジック
* **Refreshボタン押下時の挙動:**
    1. 前回の分析に使用した `cache_id` を確認。
    2. `cache_id` が有効な場合、キャッシュ以降の「数日分の差分データ」のみをプロンプトとして送信。
    3. キャッシュが期限切れの場合、全データを再取得し、新しいキャッシュを作成して分析を実行。

### 3. 取得対象メトリクス
* 体重、歩数、血圧に加え、将来的に睡眠ステージや心拍変動などの高頻度データへの拡張を想定（キャッシュ機能があるため、データ量増加に強い）。

### 4. AI推論 (Gemini 1.5 Pro)
* **大容量コンテキストの直接参照:** キャッシュされた過去の全数値データを、AIがプロンプトの一部として直接参照します。
* **分析精度:** 要約（アプローチA）とは異なり、AIが「3ヶ月前の特定の日の血圧」まで正確に把握した上で、最新データとの相関を分析できます。

---

## ■ UI設計

### 1. メイン画面
* **分析実行 (Refresh) ボタン:**
    * 内部的に「キャッシュ利用」か「フルリロード」かを自動判定し実行。
    * 処理のフェーズ（データ取得中、キャッシュ生成中、AI分析中）をユーザーに明示。
* **分析ステータス表示:**
    * 「過去◯ヶ月分のデータに基づいた最新分析」といった、参照範囲の広さをユーザーに示し信頼感を醸成。

---

## ■ データフロー図 (アプローチB)
1. [User] Refreshボタン押下
2. [App] 既存の `cache_id` の有効性をチェック
3. [App] 
   - **有効な場合:** 差分データ（JSON）を `cache_id` と共に送信
   - **無効な場合:** 過去全データを送信し、新規 `cache_id` を生成
4. [Gemini] キャッシュされた過去データ ＋ 差分データを統合して推論
5. [App] 分析結果を表示し、新しい `last_synced_timestamp` と `cache_id` を保存

---

## ■ コスト・パフォーマンスの考慮
* **入力トークン削減:** 差分送信のみになるため、通信量と入力トークン料金を大幅に削減。
* **キャッシュ料金:** キャッシュの保存時間（Storage Time）に応じた料金が発生するため、データの同期頻度に基づいた最適な TTL 設定を行う。